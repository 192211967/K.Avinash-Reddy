#include<stdio.h>
int main()
{
	int i,j,rows;
	printf("enter the numbe of rows :");
	scanf("%d",&rows);
	for(i=1;i<=rows;++i)
	{
		for(j=1;j<=i;++j)
		{
			if(i==j||i==rows||j==1)
			{
				printf("*");
		    }
		    else
		    {
		    	printf(" ");
			}
		}
		printf("\n");
	}
	return 0;
