#include <stdio.h>
int main() 
{
    int i,arr[5] = { 10, 20, 30, 40, 50 },sum=0;
    printf("The numbers are:\n");
    for ( i = 0; i < 5; i++) 
	{
        printf("%d ", arr[i]);
        sum += arr[i];
    }
    printf("\nThe sum is: %d\n", sum);
    return 0;
}
